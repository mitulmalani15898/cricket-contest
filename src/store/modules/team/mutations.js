export default {
    setTeamsLength(state, teamsLength) {
        state.teamsLength = teamsLength;
    },
    setTeams(state, teams) {
        state.teams = teams;
    },
    setAllTeams(state, teams) {
        state.allTeams = teams;
    }
}